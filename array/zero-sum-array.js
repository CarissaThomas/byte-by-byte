//Given an array write a function to find any subarray that sums to 0 if one exists

function zeroSum(arr) {
    var sum = 0;

    for (let index = 0; index < array.length; index++) {
        const element = array[index];
        
    }
}